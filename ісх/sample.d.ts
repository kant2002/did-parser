declare module "expect/build/matchers" {
    const matchers: jest.Matchers<any>;
    export default matchers;
}

declare module "expect/build/toThrowMatchers" {
    const matchers: jest.Matchers<any>;
    export default matchers;
}
